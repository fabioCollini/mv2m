machine:
  java:
    version: oraclejdk8

dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "android-23"
    - echo y | android update sdk --no-ui --all --filter "build-tools-23.0.1"

test:
  override:
    - ./gradlew :demo:test :demo:jacocoUnitTestReport
    - echo no | android create avd --force -n emulator_21 -t android-21 --abi armeabi-v7a --skin WVGA800
    # start the emulator
    - emulator -avd emulator_21 -no-skin -no-audio -no-window:
        background: true
        parallel: true
    # wait for it to have booted
    - circle-android wait-for-boot
    # run tests  against the emulator.
    - ./gradlew :demo:connectedAndroidTest
    # copy the build outputs to artifacts
    - cp -r mv2m/build/outputs $CIRCLE_ARTIFACTS
    # copy the test results to the test results directory.
    - cp -r mv2m/build/outputs/androidTest-results/* $CIRCLE_TEST_REPORTS